import { Typography } from '@mui/material';
import styled from 'styled-components';

export const PrimaryHeader = styled(Typography)(() => ({
    '&&&': {
        color: '#E72565',
        fontSize: '30px',
        fontWeight: 'bold',
        textTransform: 'capitalize',
    },
}));

export const SecondaryHeader = styled(Typography)(() => ({
    '&&&': {
        color: '#170F49',
        fontSize: '30px',
        fontWeight: 'bold',
        textTransform: 'capitalize',
    },
}));

export const PrimarySubHeader = styled(Typography)(() => ({
    '&&&': {
        color: '#E72565',
        fontSize: '25px',
        fontWeight: 'bold',
        textTransform: 'capitalize',
    },
}));

export const SecondarySubHeader = styled(Typography)(() => ({
    '&&&': {
        color: '#170F49',
        fontSize: '25px',
        fontWeight: 'bold',
        textTransform: 'capitalize',
    },
}));

export const PrimarySmallText = styled(Typography)(() => ({
    '&&&': {
        color: '#E72565',
        fontSize: '12px',
        fontWeight: 'bold',
        textTransform: 'capitalize',
    },
}));

export const SecondarySmallText = styled(Typography)(() => ({
    '&&&': {
        color: '#170F49',
        fontSize: '12px',
        fontWeight: 'bold',
        textTransform: 'capitalize',
    },
}));

export const PrimaryMediumText = styled(Typography)(() => ({
    '&&&': {
        color: '#E72565',
        fontSize: '16px',
        fontWeight: 'bold',
        textTransform: 'capitalize',
    },
}));

export const SecondaryMediumText = styled(Typography)(() => ({
    '&&&': {
        color: '#170F49',
        fontSize: '16px',
        fontWeight: 'bold',
        textTransform: 'capitalize',
    },
}));

export const PrimaryLargeText = styled(Typography)(() => ({
    '&&&': {
        color: '#E72565',
        fontSize: '18px',
        fontWeight: 'bold',
        textTransform: 'capitalize',
    },
}));

export const SecondaryLargeText = styled(Typography)(() => ({
    '&&&': {
        color: '#170F49',
        fontSize: '18px',
        fontWeight: 'bold',
        textTransform: 'capitalize',
    },
}));
